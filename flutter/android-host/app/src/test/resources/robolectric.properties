# Robolectric needs Java 21 to run SDK 36, and the project builds with JDK 17
# (like sample-expo-brownfield), so the tests run on SDK 35.
sdk=35

# HostApplication starts a Flutter engine, which loads libflutter.so; the JVM
# the tests run on has no such library. The tests need no engine.
application=android.app.Application
